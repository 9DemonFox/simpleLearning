# 显示属性
labelConfig = {
    "borderwidth": 1,
    "width": None,
    "height": None,
    "relief": "sunken",  # flat(默认),sunken,raised,groove,ridge
    "background": "red",  # 前景
    "foreground": "blue",
    "justify": "center",  # center(默认)left/right/
    "anchor": "center",  # n/s/w/e/ne/nw/sw/se/center(默认)
}

# 布局属性
labelGridConfig = {

}
